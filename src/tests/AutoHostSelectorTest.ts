import { assert } from 'chai';
import { AutoHostSelector, Lobby, LobbyStatus, logIrcEvent, Player, ILobby } from "../models";

export function AutoHostSelectorTest() {
  it("constructor test", () => {
    
  }); 
}